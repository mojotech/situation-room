* Change how dispatcher.go and worker.go functional
  - It shouldnt select all sites at each interval and process them in a loop
  - We should start a channel per site that waits/times out itself based on the duration
* Update CORS config to not always be global wildcard allow
* Fix Makefile gin command to handle unset PORT env var
* Replace/update Gorp to latest
* Don't use sqlite DB in production?
* Clean/truncate/remote old records from DB checks table
